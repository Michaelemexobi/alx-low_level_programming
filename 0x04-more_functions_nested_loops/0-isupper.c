#include "main.h"
/**
 * _isupper -prog checks for upper character
 * @c:Variable text
 * Return: 0 al ways successful
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
